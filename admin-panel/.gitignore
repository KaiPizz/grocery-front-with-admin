# reinstallable
/node_modules
/.next/
next-env.d.ts

# TypeScript incremental build cache (machine-specific, regenerated by tsc)
tsconfig.tsbuildinfo

# Local admin credential state (contains the password hash and session generation)
/data/.auth/
