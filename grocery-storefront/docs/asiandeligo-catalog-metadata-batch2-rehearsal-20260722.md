# Asia Deli Go catalog metadata batch 2 rehearsal — 2026-07-22

## Scope

- Batch: `asiandeligo-catalog-metadata-batch2-20260722-v1`
- Tenant: `e73271a9-53e3-4a20-a02e-791726b452aa`
- Channel: `asiandeligo`
- Reviewed queue slice: ranks 26–50, 25 exact SKUs
- High-confidence transitions: 24 products / 36 field changes
- Evidence hold: `ADG-000176`
- Decision SHA-256: `792e0c4277c96bc5a49edcd988b268ea178c0d93176ddfd5d3dd770fe2922732`
- Apply SQL SHA-256: `8f8696f1bd0f5fd8fcaf5b367666f0d5218fc6a7c2b41aa6841befdfffdc6d45`
- Rollback SQL SHA-256: `ea421070127b0d22a31444b1cde901f9bac6bdf46cc85758a722f40aad8817bb`
- Production access during preparation and rehearsal was read-only.

Only six `products` metadata columns are writable: `allergens`,
`may_contain_allergens`, `storage_zone`, `nutrition_facts`,
`country_of_origin`, and `ingredients`. The reviewed set changes four allergen
declarations, two trace declarations, 19 storage zones, four nutrition panels,
five countries of origin, and two ingredients values. Price, unit price,
stock, category, name, slug, publication state, variants, and categories are
outside the mutation.

Independent review removed two unsupported values before rehearsal:

- `łopian` on the exact `ADG-000405` label means burdock, not lupin, so
  `lupin` is not written;
- exact-EAN sources disagree on `ADG-000174` salt, so salt and a threshold-only
  sugar value are omitted from the nutrition object.

`ADG-000176` is held because its production EAN differs from the exact
Ourhome evidence EAN already assigned to `ADG-000177`; no duplicate identity
is guessed.

## Production-shape fixture

A disposable PostgreSQL database was created locally from a read-only current
production schema dump and populated through explicit non-generated column
lists with the active channel, 11 referenced categories, 24 exact products,
and 24 exact variants. The four relevant schema signatures and the tracked
pre-state matched production exactly:

| Table | Columns | Ordered schema signature |
| --- | ---: | --- |
| `categories` | 38 | `b33b84175ac1c2c711cef23edf28e25c` |
| `channels` | 39 | `f4d731f506e2f7ea2a0585e2ddb99891` |
| `product_variants` | 134 | `1216beeba694ed662a1c93359c56f1b8` |
| `products` | 178 | `b7fad637c5cea94d9b0d1468b5b4e485` |

The tracked metadata/price/category/variant signature was
`13dfbbb294afe9b5f8b9efa256d4a097`. The required monotonic
`products.updated_at` trigger was present and enabled.

## Rehearsal results

| Case | Result | Evidence |
| --- | --- | --- |
| Happy-path apply | PASS | Transaction `199517656` committed exactly 24 backups and 24 updates; all 24 applied timestamps advanced. |
| Exact target verification | PASS | 24/24 rows matched every target metadata field; field counts were allergens 4, may-contain 2, storage 19, nutrition 4, country 5, ingredients 2. |
| Out-of-scope invariants | PASS | Price/unit/category matched 24/24 and all 24 variant stock values remained exactly `100`. |
| Duplicate apply | PASS (rejected) | Exit `3`; existing dated backup/audit tables caused a full abort and counts remained `24/2`. |
| Happy-path rollback | PASS | Transaction `199517658` restored all 24 products; metadata signature returned exactly to `13dfbbb294afe9b5f8b9efa256d4a097`, with newer rollback timestamps. |
| Duplicate rollback | PASS (rejected) | Exit `3`; the existing `rollback_complete` audit caused a full abort. |
| Stale pre-apply row | PASS (rejected atomically) | A local unit-price edit reduced exact locks to 23. Exit `3`; no backup/audit relation was created and pre/post attempt signature stayed `80c143edd5088726fb8b855c8e8378d1`. |
| Intervening product edit | PASS (rollback rejected atomically) | A local post-apply ingredient marker reduced target locks to 23. Exit `3`; the marker remained and rollback audit count stayed zero. |
| Tampered persistent backup | PASS (rollback rejected atomically) | A local backup-price edit failed the exact backup snapshot guard. Exit `3`; target products remained and rollback audit count stayed zero. |

The generated SQL pins `search_path`, uses schema-qualified persistent
relations, runs at `SERIALIZABLE`, locks exact product/variant/category rows,
checks tenant, channel, IDs, SKU, status, category, metadata, price/unit, and
`updated_at`, and never deletes or drops data.

## Automated and live read-only verification

- Focused batch-2 generator suite: **4/4 passed**.
- Full catalog audit suite: **33/33 passed**.
- Artifacts regenerated byte-for-byte from the decision file.
- `node --check` and `git diff --check`: passed.
- Fresh production read: 24/24 rows, 24/24 timestamps, 24/24 stock values,
  signature `13dfbbb294afe9b5f8b9efa256d4a097`.
- Exactly one active channel and one enabled timestamp trigger existed; both
  dated backup/audit relations were absent.
- All 24 PDPs, storefront homepage, both health endpoints, login, and mail
  inbox returned HTTP 200.

No production database write, source transfer, application build, deploy, or
service restart occurred during rehearsal.

## Cleanup

The base fixture and four case databases were explicitly removed after
verification. A final lookup returned zero matching rehearsal databases.
