# node_modules (reinstall with: npm install)
node_modules/

# Next.js build cache (regenerated on: npm run dev)
.next/
.next-stale/

# OS junk
.DS_Store
Thumbs.db

# Google Drive sync artifacts
.tmp.driveupload/
.vscode/

# Claude Code local configuration (skills, agents, rules, commands, frameworks)
# These are machine-local and should not be committed
.claude/
CLAUDE.md
